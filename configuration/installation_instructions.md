# n8n MCP Server Setup

This extension connects Zed to your **instance-level n8n MCP server** via the
Model Context Protocol (MCP). Set `server_url` to your instance endpoint, which
looks like `https://<your-n8n-host>/mcp-server/http` (see n8n's Settings →
Instance-level MCP page for the exact URL).